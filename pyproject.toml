[project]
name = "sat-qkd-security-curves"
version = "0.1.0"
description = "BB84 QKD simulation + satellite-style link loss model + security curves"
requires-python = ">=3.10"

[project.dependencies]
numpy = "*"
matplotlib = "*"
